const express = require('express')
const colors = require('colors');
const dotenv = require('dotenv').config();

const PORT = 4000

const app = express()


app.listen(PORT, () => {
    resizeBy.send({message:"hello"})
})